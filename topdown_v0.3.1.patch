diff --git a/a b/a
index 532bb72..cb71909 100755
--- a/a
+++ b/a
@@ -35,7 +35,9 @@ zip -r "${archive}" . \
      ".ruff_cache/*" \
      "dist/*" \
      "build/*" \
-     "*.egg-info/*"
+     "*.egg-info/*" \
+     "logs/*.log" \
+     "${PROJECT_NAME}_v*_current.zip"
 
 echo
 echo "Done:"
diff --git a/pyproject.toml b/pyproject.toml
index 2b7c747..4c1d1ac 100644
--- a/pyproject.toml
+++ b/pyproject.toml
@@ -4,7 +4,7 @@ build-backend = "setuptools.build_meta"
 
 [project]
 name = "topdown-shooter"
-version = "0.3.0"
+version = "0.3.1"
 description = "Step 3 playable core for a procedural top-down shooter on Pyray."
 requires-python = ">=3.12"
 dependencies = [
diff --git a/src/topdown/__init__.py b/src/topdown/__init__.py
index 6b067c3..c28e6da 100644
--- a/src/topdown/__init__.py
+++ b/src/topdown/__init__.py
@@ -1 +1,3 @@
 """Top-down shooter prototype package."""
+
+__version__ = "0.3.1"
diff --git a/versions.md b/versions.md
index a509dcf..8ca4d8c 100644
--- a/versions.md
+++ b/versions.md
@@ -1,6 +1,12 @@
-# TopDownMapGen versions
+# TopDownShooter versions
 
 ## v0.0.0 -> v0.0.1
 
 - Init repo with sources.
 
+
+## v0.3.0 -> v0.3.1
+
+- Согласована версия проекта между `pyproject.toml`, пакетом `topdown` и журналом версий.
+- Добавлен экспорт `topdown.__version__`.
+- Скрипт архивации дополнительно исключает runtime-логи и локальные архивы проекта.
